using System;

namespace IdentityManager.Models
{
namespace IdentityManager.Models
{
    public class ErrorViewModel
    {
        public string RequestId { get; set; }

            public bool ShowRequestId => !string.IsNullOrEmpty(RequestId);//if not null ShowRequestId =true and show in application user
        }
}

}
